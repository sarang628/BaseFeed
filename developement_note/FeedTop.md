## [FeedTop](../library/src/main/kotlin/com/sarang/torang/compose/feed/internal/components/FeedTop.kt)

<img src = "../screenshot/feedtop.png" width = "500">

- Compose의 Composable 특징을 정리하며 구현

- composable의 특징
- 앱의 UI란 '화면에 보이는 것'
- Jetpack Compose는 안드로이드 UI를 만드는 modern toolkit
- Compose는 UI개발을 '적은 코드', '강력한 도구', '직관적인 코틀린 가용성'으로 '단순'하고 '빠르게' 해줌.

- Describes some part of your UI.
- Doesn't return anything.
- Takes some input and generates what's shown on the screen.


Annotations are means of attaching extra information to code.
Annotations can take parameters.

Jetpack Compose is built around composable functions.
Composable functions can accept arguments, which let the app logic describe or modify the UI.

MUST be a noun: DoneButton()